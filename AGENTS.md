# Widamine — Agent Guide

See **[docs/development/agents.md](docs/development/agents.md)** for setup, architecture, conventions, and troubleshooting.

Quick start:

```bash
./start-mongodb.sh && npm run bootstrap && npm run db:seed && npm run dev
```
