model User {
    id       Int       @id() @default(autoincrement())
    name     String
    email    String    @unique
    password String
    role     Role
    services Service[] @relation("DoctorServices")
}

enum Role {
    ADMIN
    DOCTOR
    PRACTITIONER
    RECEPTIONIST
}
